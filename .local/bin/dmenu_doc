#!/bin/sh
# Program:
#	* Select and open cheatsheet
# Dependency:
# 	* dmenu(1)
# Author:
#       * Chang, Chu-Kuan <cckuan@changchukuan.name>

readonly csdir="${HOME}/documents/"

cd "${csdir}" || exit 1

file="$(
	find . -type f -maxdepth 2 \
	    | sed 's|^\./||' \
	    | dmenu -i -l 5 -p 'Which document to open?'
)"

[ -f "${file}" ] && xdg-open "${file}"

